☐ fix animations in sticky footer with reveal

☐ three.js jouney to make 3d; use HEYCLICKY to make it correct colour

☐ text reveal animations on scroll for headings
☐ text stagger animations on all texts (check osmo and Monolog and Paronyme for examples)

☐ check if I should stick to using lg: as main breakpoint or use md: instead

Events page
☐ make the hover interaction snappier like bymonolog
☐ the scrollable list for events: https://www.edicioneselfuerte.com/
☐ add some type of effect either on the custom cursor or on the list items, when hovering them


CTA's
☐ for CTA hover transitions: maybe we can use a blur flicker through ? where the letters translate slighly and blur from left to right ?
☐ MAKE SURE ALL LINKS (CTA LINKS) GO TO CORRECT PAGES AND HAVE MICRO INTERACTIONS

☐ remove all inline styles from the pages and components

☐ emil design engineering

☐ footer parallax animation is fucked up at the moment
☐ when you navigate to a different page, its fine on reload

☐ add parallax to all sections, and perhaps even alternate dark and light sections?
but add parallax for sure, get inspo from homepage: by monolog

☐ the opening and closing of rows in events list is weird at the moment, and not smooth

☐ add proper transition to ServicesGrid.vue section for the iamges (check osmo)

☐ add webhooks in sanity and vercel for immediate content updates
=> just like for ana project
=> deploy local sanity studio (have a tab open on this, in codex)
Create the Revalidate webhook (needs your deployed HTTPS URL)? … yes

   Sanity delivers this webhook from its cloud, so it must point at a public URL (localhost and private hosts are rejected).
   No deployment yet? Keep the example.com placeholder to register it now, then repoint it once you deploy.

✔ Public site URL (https://…, not localhost). Full webhook = base + /api/revalidate … https://example.com

ℹ️  https://example.com is a placeholder, so the webhook registers but will not deliver. Repoint it at your deployed HTTPS URL in Manage → API → Webhooks (or re-run this setup after deploying).

☐ SEO check!! pagespeed insights and fix

☐ french version of impressum page

☐ TODOS throughout page

pressed states on buttons
    my go-to is a simple scale(0.99) on press. tiny change, big difference. gives you that clicky ux feedback.
